/**
 * Minimal loading hooks (console-only for now).
 * @param {string} message
 */
export function showLoading(message) {
  console.log(`⏳ ${message}`);
}

export function hideLoading() {
  // Placeholder for future loading overlay.
}

